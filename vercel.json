{
  "buildCommand": "cd server && npm install && echo DATABASE_URL=file:./dev.db > .env && echo JWT_SECRET=smartedu-default-jwt-secret-2026 >> .env && echo AI_PROVIDER=built-in >> .env && npx prisma generate && npx prisma db push && node prisma/seed.js && cd .. && cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "functions": {
    "api/index.js": {
      "includeFiles": "server/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
